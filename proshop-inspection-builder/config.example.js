// Copy this file to `config.js` (same folder as index.html) and fill in your real key.
// `config.js` is gitignored and must NEVER be committed.
// If `config.js` is missing at runtime, the onerror handler in index.html sets
// window.PSB_CONFIG.anthropicApiKey to null and Claude OCR fallback is disabled silently.
window.PSB_CONFIG = {
  anthropicApiKey: 'sk-ant-PLACEHOLDER-REPLACE-ME'
};
